# Anime-Cards
A full‑stack web application built with Spring Boot as the backend, React as the frontend, and MySQL as the database. The system is containerized with Docker and orchestrated via Docker Compose, ensuring smooth integration between services. Continuous Delivery is automated using Jenkins, enabling reliable builds, tests, and deployments.
